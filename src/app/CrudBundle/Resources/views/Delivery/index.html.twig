{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Delivery list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Ref</th>
                <th>Datedelivery</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('command_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.ref }}</td>
                <td>{% if entity.dateDelivery %}{{ entity.dateDelivery|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('command_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('command_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('command_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
